# This file is auto-generated by the Mailu configuration wizard.
# Please read the documentation before attempting any change.
# Generated for compose flavor

version: '2.2'

services:

  # External dependencies
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - "/home/stark/docker/mailu/data/redis:/data"

  # Core services
  front:
    image: leolivier/mailu-nginx:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}nginx:${MAILU_VERSION:-1.9}
    restart: always
    env_file: mailu.env
    logging:
      driver: json-file
    ports:
      - "880:80"
      - "9443:443"
      - "25:25"
      - "465:465"
      - "587:587"
      - "110:110"
      - "995:995"
      - "143:143"
      - "993:993"
    volumes:
      - "/home/stark/docker/mailu/data/certs:/certs"
      - "/home/stark/docker/mailu/data/overrides/nginx:/overrides:ro"

  admin:
    image: leolivier/mailu-admin:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}admin:${MAILU_VERSION:-1.9}
    restart: always
    env_file: mailu.env
    volumes:
      - "/home/stark/docker/mailu/data/data:/data"
      - "/home/stark/docker/mailu/data/dkim:/dkim"
    depends_on:
      - redis

  imap:
    image: leolivier/mailu-dovecot:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}dovecot:${MAILU_VERSION:-1.9}
    restart: always
    env_file: mailu.env
    volumes:
      - "/home/stark/docker/mailu/data/mail:/mail"
      - "/home/stark/docker/mailu/data/overrides/dovecot:/overrides:ro"
    depends_on:
      - front

  smtp:
    image: leolivier/mailu-postfix:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postfix:${MAILU_VERSION:-1.9}
    restart: always
    env_file: mailu.env
    volumes:
      - "/home/stark/docker/mailu/data/mailqueue:/queue"
      - "/home/stark/docker/mailu/data/overrides/postfix:/overrides:ro"
    depends_on:
      - front

  antispam:
    image: leolivier/mailu-rspamd:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}rspamd:${MAILU_VERSION:-1.9}
    hostname: antispam
    restart: always
    env_file: mailu.env
    volumes:
      - "/home/stark/docker/mailu/data/filter:/var/lib/rspamd"
      - "/home/stark/docker/mailu/data/overrides/rspamd:/etc/rspamd/override.d:ro"
    depends_on:
      - front

  # Optional services

  webdav:
    image: leolivier/mailu-radicale:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}radicale:${MAILU_VERSION:-1.9}
    restart: always
    env_file: mailu.env
    volumes:
      - "/home/stark/docker/mailu/data/dav:/data"

  fetchmail:
    image: leolivier/mailu-fetchmail:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}fetchmail:${MAILU_VERSION:-1.9}
    restart: always
    env_file: mailu.env
    volumes:
      - "/home/stark/docker/mailu/data/data/fetchmail:/data"

  # Webmail
  webmail:
    image: leolivier/mailu-rainloop:latest
    #${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}rainloop:${MAILU_VERSION:-1.9}
    restart: always
    env_file: mailu.env
    volumes:
      - "/home/stark/docker/mailu/data/webmail:/data"
      - "/home/stark/docker/mailu/data/overrides/rainloop:/overrides:ro"
    depends_on:
      - imap

networks:
  default:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 192.168.203.0/24
